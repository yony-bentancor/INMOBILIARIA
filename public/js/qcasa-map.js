(function(){const el=document.getElementById('qcPropertyMap');if(!el||!window.L)return;const items=window.QCASA_MAP_PROPERTIES||[],side=document.getElementById('qcMapSelected');const map=L.map(el).setView([-34.46,-57.84],10);L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,attribution:'© OpenStreetMap'}).addTo(map);const bounds=[];const esc=v=>String(v??'').replace(/[&<>"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[m]));const card=p=>`<article class="qc-map-card"><img src="${esc(p.image)}" alt=""><div><small>${esc(p.operation)} · ${esc(p.category)}</small><h3>${esc(p.title)}</h3><strong>${esc(p.priceLabel)}</strong><p>${esc(p.city)} · ${esc(p.area)} m²${p.bedrooms?` · ${esc(p.bedrooms)} dorm.`:''}</p><div class="qc-map-card-actions"><a href="/qcasa/propiedad/${encodeURIComponent(p.slug)}">Ver propiedad</a><a target="_blank" rel="noopener" href="${esc(p.shareWhatsApp)}">WhatsApp</a></div></div></article>`;items.forEach(p=>{if(!Number.isFinite(+p.lat)||!Number.isFinite(+p.lng))return;const m=L.marker([+p.lat,+p.lng]).addTo(map);m.bindTooltip(p.priceShort,{permanent:true,direction:'top',className:'qc-price-marker'});m.on('click',()=>side.innerHTML=card(p));bounds.push([+p.lat,+p.lng]);if(window.QCASA_MAP_FOCUS===p.id)setTimeout(()=>{map.setView([+p.lat,+p.lng],15);side.innerHTML=card(p)},100)});if(bounds.length&&!window.QCASA_MAP_FOCUS)map.fitBounds(bounds,{padding:[35,35],maxZoom:12});})();