# Subir QCASA a GitHub

```bash
git init
git add .
git commit -m "MVP QCASA"
git branch -M main
git remote add origin https://github.com/TU_USUARIO/qcasa.git
git push -u origin main
```

El archivo `.env` está ignorado por Git. Usá `.env.example` como plantilla pública.
