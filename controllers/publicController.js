const store=require('../data/demoStore');const{uid,nowISO,slugToken}=require('../utils/helpers');const{CLAIM_CATEGORIES}=require('../config/constants');
exports.home=(req,res)=>res.render('public/home.njk',{title:'QCASA'});
exports.qrLanding=(req,res)=>{const p=store.findProperty(req.params.code);if(!p||!p.active)return res.status(404).send('Propiedad no encontrada.');res.render('public/report.njk',{title:`Reportar problema | ${p.address}`,property:p,categories:CLAIM_CATEGORIES})};
exports.createComplaint=(req,res)=>{const p=store.findProperty(req.params.code);if(!p||!p.active)return res.status(404).send('Propiedad no encontrada.');const number=store.nextComplaintNumber();const attachments=(req.files||[]).map(f=>({id:uid('att'),kind:f.mimetype.startsWith('video/')?'video':'image',name:f.originalname,url:`/uploads/${f.filename}`}));const c={id:uid('clm'),number,propertyCode:p.code,category:req.body.category||'Otros',title:req.body.title||req.body.category||'Problema reportado',description:req.body.description||'',phone:p.tenant?.phone||req.body.phone||'',priority:req.body.priority||'medium',status:'Nuevo',technicianId:null,technicianName:null,technicianEmail:null,attachments,shareToken:slugToken(),createdAt:nowISO(),updatedAt:nowISO(),history:[{at:nowISO(),status:'Nuevo',note:'Reclamo recibido desde QR público.'}]};store.complaints.unshift(c);store.addAudit(p.code,'Inquilino vía QR','Nuevo reclamo',`Reclamo #${number}: ${c.title}`);res.redirect(`/reclamo-enviado/${number}`)};
exports.success=(req,res)=>{const c=store.complaints.find(x=>String(x.number)===String(req.params.number));if(!c)return res.status(404).send('Reclamo no encontrado.');res.render('public/success.njk',{title:'Reclamo enviado | QCASA',complaint:c})};
exports.publicJob=(req,res)=>{const c=store.complaints.find(x=>x.shareToken===req.params.token);if(!c)return res.status(404).send('Trabajo no encontrado.');res.render('public/job.njk',{title:`Trabajo #${c.number} | QCASA`,complaint:c,property:store.findProperty(c.propertyCode)})};
